# count

How to invoke:
`count`

What it does:
Count rows in the active dataset.

What problem it answers:
How many observations are available right now?

Examples:
- `use patients.parquet`
- `count`
