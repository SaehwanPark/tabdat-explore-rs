# histogram

How to invoke:
`histogram varname [, bins=n saving(path) noopen]`

What it does:
Save a histogram plot for one numeric variable.

What problem it answers:
What does the distribution of a numeric variable look like?

Examples:
- `histogram age`
- `histogram age, bins=20`

Links:
- `docs/project_proposal.md`
