# tail

How to invoke:
`tail [n]`

What it does:
Preview the last `n` rows of the active dataset in their original relative order. `tail 0` returns
no rows.

What problem it answers:
What does the bottom of the data look like?

Examples:
- `tail`
- `tail 10`
