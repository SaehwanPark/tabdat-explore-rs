# head

How to invoke:
`head [n]`

What it does:
Preview the first `n` rows of the active dataset in its current row order. `head 0` returns no rows.

What problem it answers:
What does the top of the data look like?

Examples:
- `head`
- `head 10`
