[package]
name = "tabdat-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
duckdb = { version = "1.10505.0", features = ["bundled", "parquet"] }
sha2 = "0.10"
tabdat-language = { path = "../tabdat-language" }
